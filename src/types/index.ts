export type ActorInfo = {
  photo: string;
  name: string;
  link: string;
  birthDay: string;
  birthPlace: string;
};
export type WikiActorInfo = {
  photo: string;
  name: string;
  link: string;
};
